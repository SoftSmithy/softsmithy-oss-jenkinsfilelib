{
  "$id": "http://www.drombler.org/drombler-media-core/Definitions.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "definitions": {
    "maven": {
      "$id": "/definitions/maven",
      "type": "object",
      "properties": {
        "installationName": {
          "$id": "/definitions/maven/properties/installationName",
          "title": "The installation name (version)",
          "description": "The name (version) of the Maven installation configuration in Jenkins",
          "type": "string"
        }
      }
    },
    "java": {
      "$id": "/definitions/java",
      "type": "object",
      "properties": {
        "installationName": {
          "$id": "/definitions/java/properties/installationName",
          "title": "The installation name (version)",
          "description": "The name (version) of the Java installation configuration in Jenkins",
          "type": "string"
        }
      }
    },
    "vcs": {
      "$id": "/definitions/vcs",
      "type": "object",
      "properties": {
        "type": {
          "$id": "/definitions/vcs/properties/type",
          "title": "The VCS type",
          "type": "string",
          "enum": [
            "GIT"
          ]
        }
      }
    },
    "codeHostingPlatform": {
      "$id": "/definitions/codeHostingPlatform",
      "type": "object",
      "properties": {
        "type": {
          "$id": "/definitions/codeHostingPlatform/properties/type",
          "title": "The code hosting platform type",
          "type": "string",
          "enum": [
            "GITHUB"
          ]
        }
      }
    }
  }
}